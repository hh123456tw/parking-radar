# 修正紀錄

本檔只記錄會影響展示與使用者判斷的變更，避免混入細小重構。

## 2026-08-23

### 實際步行距離

- 地址查詢先以 1.5 公里直線距離篩選，再優先對低風險且較近的 15 座呼叫一次 OpenRouteService 步行矩陣。
- 推薦仍先依空位風險分級；同風險場站改以停好車後的步行時間排序。
- 圖卡、精簡列表及地圖明確顯示「步行約幾分鐘」；沒有步行結果時標示為「直線約」。
- 路線服務逾時、回傳異常或未設定金鑰時，自動沿用原本直線距離，不中斷停車查詢。

## 2026-08-20

### 抵達日與費率分類

- 卡片顯示抵達日分類（平日／週末／國定假日），行事曆資料由每月維護任務定期下載。
- 顯示結構化每小時或每計次費率，並以保守上限呈現，避免高估可停車時間。
- 費用與設施型態依資料來源優先順序填補（先官方後第三方），並標示來源。
- 無法判斷的值明確顯示「未知」，不會替使用者捏造數字。

### 安裝與離線外殼

- 提供可安裝 PWA：Android Chrome 與 iOS Safari 可加入主畫面，離線時仍顯示應用外殼。
- 一般查詢仍然直接走網路，不讀取服務工人快取。

### 優雅降級

- 行事曆或第三方資料來源暫時失敗時仍維持既有停車推薦，不因裝飾性功能中斷查詢。

## 2026-08-04

### 模糊地標與多地點

- 沒有完整門牌的模糊地標即使只驗證出一筆，也必須先由使用者確認，不再自動選定。
- Gemini 最多提出三個正式地址候選，後端再用 Nominatim 驗證。
- 模糊地標的有效候選一律顯示前端按鈕；沒有有效候選才要求完整地址。
- 候選點選沿用既有手動查詢，不建立第二套停車分析流程。
- 固定別名只保留台北車站與臺北市政府等穩定展示案例。
- 舊分頁遇到新版候選回應時改為提示重新整理，避免顯示 JavaScript 英文錯誤。
- 靜態 JavaScript 與 CSS 網址加入版本，重新整理後不會沿用舊檔。

### 推薦排序

- 改為先分風險，再於同風險中按照距離排序。
- 0～3 格不建議前往；4～10 格或空位率低於 10% 列為備選。
- 低風險場站不足三個時才補入黃色備選，卡片與地圖維持黃色標示。

### 地址與地圖

- 修正地標查詢重複補上「臺北市」而定位錯誤的問題。
- 台北車站改用固定門牌，並清除曾指向東門國小的錯誤快取。
- 停車場名稱與地址可直接開啟 Google 地圖。

### 即時資料與畫面

- 查詢時發現資料超過 45 分鐘會嘗試更新；更新失敗時顯示舊資料提醒。
- 修正地點圖卡流程重用更新前 MySQL 交易，導致補抓成功後仍顯示舊空位的問題。
- 結果改為三張主要卡片、其他場站精簡列表及醒目的地圖編號。
- 歷史圖改為點擊後載入，少於八筆只顯示資料累積提示。

### Gemini 穩定性

- 主模型忙碌時改用備援 Flash Lite 模型。
- 地標名稱、行政區與抵達時間使用固定結構驗證，Gemini 不參與停車排名。
